# Capability frontmatter schema

Documents the YAML frontmatter shape every capability file must declare. Not a
capability itself — `load_capabilities` must skip it the same way it skips
README.md, even though the H1 is valid.
