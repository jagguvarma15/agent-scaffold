# Event Driven Recipe

This recipe describes an event-driven agent that reads from a queue. It uses the
Event Driven pattern for async processing — no explicit link to the pattern doc
is included in this fixture on purpose, so the alias resolver has to find it.
