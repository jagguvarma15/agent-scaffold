---
status: blueprint
languages: [python]
recipe_dependencies:
  python:
    anthropic: ">=99.0.0"
---

# Conflict Recipe Deps

Recipe declaring a version for a package already in language defaults; the
recipe value should win on conflict.
